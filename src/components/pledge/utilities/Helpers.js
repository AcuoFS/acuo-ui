import _ from 'lodash'

const CollSort = ( CollSort_Arg )=>{
 const { All_Collaterals, SortBy } = CollSort_Arg

 console.log(All_Collaterals);

 return { }
}

const Helper = {
 CollSort
}

export default Helper
